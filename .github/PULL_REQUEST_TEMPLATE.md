## 무엇을

<!-- 어떤 기능 또는 UI인지 한두 줄. 기능 하나만 담는다 (AGENTS.md 규칙 11) -->

## 어떻게

<!-- 어떤 방법으로 구현했는지. 핵심 파일 경로를 같이 적는다 -->

## 왜

<!-- 왜 그 방법을 골랐는지. 근거는 이 레포의 `파일:줄` 또는 실행한 명령 출력 (규칙 4) -->

## 트레이드오프

<!--
무엇을 포기했는지. 없으면 "없음"이라고 쓴다.
예) 댓글은 1뎁스까지만 — 무한 중첩은 모바일에서 안 읽히고 쿼리가 재귀로 감
-->

## 참고사항

<!-- 리뷰어가 알아야 할 것. 후속 작업, 알려진 한계, 마이그레이션 적용 필요 여부 등. 없으면 "없음" -->

## 체크리스트

- [ ] `npm run verify` 통과
- [ ] 이 PR은 기능 하나만 담는다
- [ ] 기존 함수·컴포넌트를 먼저 찾아봤고, 중복을 만들지 않았다 (규칙 1, 2)
- [ ] 서버 진입점에서 입력을 검증했다 / 권한은 RLS 정책으로 처리했다 (규칙 9)
- [ ] 로딩, 빈 상태, 없음, 에러 네 가지를 모두 처리했다 (규칙 10)
- [ ] 색·간격·폰트를 하드코딩하지 않고 `docs/DESIGN.md` 토큰을 썼다 (규칙 3)
- [ ] 스키마를 바꿨다면 타입을 다시 생성했다 (규칙 8)
- [ ] 프리뷰 URL에서 직접 눌러봤다
- [ ] `.env` / `.env.local` 값을 어디에도 옮겨 적지 않았다 (규칙 12)
- [ ] 문서를 고쳐야 한다면 무엇이 어긋났는지 아래에 적었다 (규칙 13)
