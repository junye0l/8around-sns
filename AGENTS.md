<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# 8around SNS

생각을 짧게 나누는 SNS. 3일 안에 배포까지 간다.

**이 문서가 개발 규칙의 유일한 출처다.** 사람이든 AI 에이전트든 이 문서를 읽고 작업한다.
`CLAUDE.md`는 이 파일을 가리키기만 한다. 같은 규칙을 두 곳에 적지 않는다.

- 일정: `docs/PLAN.md`
- 디자인 토큰: `docs/DESIGN.md`
- 결정 기록: `docs/decisions/` — 갈림길에서 무엇을 버렸는지

## 범위

요구 기능은 **6개**다. 이것이 전부다.

회원가입 · 로그인 · 팔로우/팔로잉 · 게시글 작성 · 댓글 · 대댓글

범위 밖(알림, DM, 좋아요, 이미지 업로드, 검색, 해시태그 등)은 **사용자 요청이 없으면 구현하지 않는다.**

## 명령어

| 명령 | 용도 |
|------|------|
| `npm run verify` | lint + typecheck + test. **커밋 전에 이것만 돌리면 된다** |
| `npm run format` | Biome 포맷 + 자동 수정 |
| `npm run dev` | 개발 서버. **에이전트가 직접 실행하지 않는다 (규칙 7)** |
| `npm run db:push` | 마이그레이션을 원격 DB에 적용 (규칙 7 — 먼저 묻는다) |
| `npm run types:gen` | 스키마에서 `types/database.ts` 재생성 |

CI는 `verify` 3단계에 `npm run build`를 더해 그대로 돌린다. 로컬에서 `verify`가 통과하면 CI도 통과한다.

## 구조

**역할별로 나눈다.** 파일을 어디에 만들지는 "이게 무슨 역할인가"로 결정한다.

```
app/                    라우트. page.tsx / layout.tsx 만. 로직을 두지 않는다
components/
  ui/                   순수 UI. props만 받는다. 데이터 접근도 상태도 없다
  layout/               화면 뼈대. 여러 화면이 공유하는 네비게이션·컨테이너
  <기능>/               그 기능에만 쓰이는 UI — post/, comment/, follow/
hooks/                  클라이언트 훅 (use~)
lib/
  supabase/             Supabase 클라이언트 생성
  queries/              읽기. 서버에서만 호출한다
  actions/              쓰기 진입점. "use server" 얇은 래퍼 — 인증 확인 후 services 호출
  services/             쓰기 로직. 검증 + DB. 프레임워크를 모른다
  utils/                순수 함수. 옆에 *.test.ts 를 둔다
types/                  스키마에서 생성한 DB 타입 (규칙 8)
supabase/migrations/    스키마. SQL 파일만이 출처 (규칙 3)
docs/                   PLAN.md, DESIGN.md
  decisions/            결정 기록. 선택지와 버린 이유 (규칙 14)
```

**어디에 둘지 헷갈릴 때**

| 이건 | 여기 |
|------|------|
| 여러 기능에서 쓸 버튼·아바타·태그 | `components/ui/` |
| 게시글에서만 쓰는 카드 | `components/post/` |
| `useState`가 두 군데 이상에서 같은 모양으로 반복될 때 | `hooks/` |
| 입력만으로 결과가 정해지는 함수 | `lib/utils/` + 테스트 |
| DB를 읽는다 | `lib/queries/` |
| DB를 쓴다 | `lib/services/` (로직) + `lib/actions/` (진입점) |

디렉터리는 **첫 파일이 생길 때 만든다.** 빈 폴더를 미리 파두지 않는다.

## 쓰기는 Server Action으로만

API 라우트를 만들지 않는다. 생성·수정·삭제는 전부 Server Action이다.

**로직은 진입점에 눌러붙이지 않는다.** 두 층으로 나눈다.

```
lib/actions/post.ts     "use server". 로그인 확인 → services 호출 → revalidate
lib/services/post.ts    검증(zod) + DB 쓰기. Next를 모른다. 테스트하기 쉽다
```

이유: 유지보수 비용은 "Next를 버릴 때"가 아니라 **로직이 진입점에 붙어서 재사용도 테스트도 안 될 때** 생긴다.
나중에 외부에서 호출할 API가 필요해지면 Route Handler가 같은 `services/` 함수를 부르면 된다. 재작성이 아니라 진입점 추가다.

## 스타일

Tailwind v4를 쓰되, **유틸리티를 화면에 직접 흩뿌리지 않고 공통 컴포넌트에 가둔다.**

- 토큰 값의 출처는 `docs/DESIGN.md`, 그것을 `app/globals.css`가 그대로 옮겨 적는다
- `@theme`로 Tailwind 유틸리티에 노출한다 — `bg-canvas` `text-fg` `text-body` `rounded-md` `border-hairline`
- **간격은 Tailwind 숫자 스케일을 쓴다.** 그것이 곧 4px 그리드다 (`p-1`=4px, `p-4`=16px, `p-16`=64px)
- **임의의 값(`p-[13px]`, `text-[#333]`)을 쓰지 않는다.** `{4,8,12,16,20,24,32,40,48,56,64}` 밖의 값이 필요하면 이유를 주석으로 남긴다 (DESIGN.md §1 Principle 5)
- 같은 UI를 두 번째로 만들 때가 아니라, **처음부터** `components/ui/`에 만든다 (규칙 1)
- `docs/DESIGN.md` §4 States는 **구현 스펙**이다. 로딩·빈 상태·에러·성공의 문구와 처리 방식이 거기 적혀 있다
- **문서에 없는 값을 지어내지 않는다** (DESIGN.md §7 Unknowns). 필요한데 없으면 멈추고 묻는다
- **라이트 모드만 지원한다.** 다크 모드는 범위 밖이다. `prefers-color-scheme` 분기를 만들지 않는다

**당근 레퍼런스에서 오는 제약** (DESIGN.md §1 Principles)

- 주황은 희소하다. **한 화면에 primary 요소 하나.** 두 개가 경쟁하면 하나는 중립으로 내린다
- 브랜드 웹폰트를 넣지 않는다. 시스템 폰트로 간다. 제목이 무거워야 하면 굵기 700을 쓴다
- 두 번째 브랜드 색을 만들지 않는다. 시맨틱 색(danger/info/success)은 유틸리티지 브랜드가 아니다
- **스프링·오버슈트 이징 금지.** `--ease-enter` / `--ease-exit` / `--ease-standard` 셋만 쓴다
- 그림자를 쓰지 않는다. 분리는 `border-hairline` 1px이 한다 (DESIGN.md §2 Depth)

---

# 규칙

## 1. 함수는 먼저 찾고, 따진 뒤, 분리한다

새 함수를 파일 안에 바로 쓰지 않는다. **파일 내부에 쓰면 분리를 누락한다.** 순서는 이렇다.

1. **찾는다** — 같은 일을 하는 함수가 이미 있는지 먼저 확인한다. 있으면 그것을 쓴다.
2. **따진다** — 이 함수가 다른 곳에서도 쓰일 수 있는지 판단한다.
3. **분리한다** — 쓰일 수 있으면 처음부터 별도 파일로 뺀다. 순수 함수로 만들고 테스트 하나를 옆에 붙인다.

한 화면의 표현에만 묶여 있고 재사용 여지가 없다고 판단되면 그 파일에 남긴다. 단, 그 판단을 **근거와 함께 말한다**(규칙 4).

## 2. 중복 코드를 만들지 않는다

공통 컴포넌트를 쓸 수 있으면 쓴다. 비슷한 UI를 새로 만들기 전에 **기존 컴포넌트를 먼저 찾는다.**
같은 모양이 세 번째로 나타나면 그때는 반드시 공통 컴포넌트로 올린다.

## 3. SOT(Single Source of Truth)를 지킨다

같은 사실이 두 곳에 적혀 있으면 **둘 다 틀린 것으로 친다.**

| 사실 | 유일한 출처 |
|------|-------------|
| DB 스키마 | `supabase/migrations/*.sql` — 대시보드에서 손으로 고치지 않는다 |
| DB 타입 | `types/database.ts` — `npm run types:gen`으로 생성한다 (규칙 8) |
| 색·간격·폰트 | `docs/DESIGN.md` 토큰 |
| 개발 규칙 | 이 문서 |
| 일정·범위 | `docs/PLAN.md` |
| 결정과 그 이유 | `docs/decisions/NNNN-*.md` (규칙 14) |

## 4. 근거 없이 코드를 쓰지 않는다

추측으로 쓰지 않는다. 근거는 **이 레포 안에서** 찾는다.

- 근거의 형태: **`파일:줄`**, 또는 **실행한 명령과 그 출력**
- "보통 이렇게 합니다", "일반적으로", "아마"는 근거가 아니다
- 외부 블로그·기억·관례를 근거로 삼지 않는다
- 레포에서 확인하지 못했으면 **확인하지 못했다고 그대로 보고한다.** 모른다고 말하는 것이 지어내는 것보다 싸다

## 5. 사실만 보고한다

했으면 했다고, 안 했으면 안 했다고 쓴다. 테스트가 실패하면 실패했다고 출력과 함께 쓴다.
"될 겁니다", "아마 동작합니다"는 보고가 아니다. **실행해서 확인한 것만 동작한다고 말한다.**

## 6. 사용자가 지시하기 전까지 임의로 진행하지 않는다

방향이 갈리는 지점에서는 멈춘다. 멈출 때는 이 형태로 말한다.

```
근거: (파일:줄 또는 명령 출력)
선택지 A — 장점 / 비용
선택지 B — 장점 / 비용
추천: A. 이유 한 줄.
```

혼자 정하고 진행한 뒤 통보하지 않는다. 관례적인 기본값이 명확한 사소한 선택은 진행하되, **무엇을 가정했는지 한 줄로 밝힌다.**

## 7. 사용자 환경을 건드리는 명령은 먼저 묻는다

사용자는 항상 `npm run dev`를 띄워 두고 있다. **개발 서버를 임의로 실행하지 않는다.** 확인이 필요하면 사용자에게 요청한다.

물어보고 실행하는 것들:

- 개발 서버 실행 / 종료, 포트 점유
- DB 마이그레이션 적용, 리셋, 시드
- `git push`, 브랜치 전환 (히스토리를 다시 쓰는 명령은 아예 금지 — "히스토리를 다시 쓰지 않는다" 절)
- 패키지 설치 / 제거

환경변수 파일은 실행이 아니라 **열람 자체가 금지**다 (규칙 12).

## 8. 스키마가 타입의 출처다

DB 타입은 스키마에서 생성한다. 손으로 쓴 타입과 실제 컬럼이 어긋나면 런타임에서만 터진다.

```
supabase/migrations/*.sql   스키마를 바꾸는 유일한 방법. 새 파일을 추가한다
npm run db:push             원격 DB에 적용
npm run types:gen           types/database.ts 재생성
```

- **`types/database.ts`를 손으로 고치지 않는다.** 생성물이다. 고쳐도 다음 `types:gen` 때 날아간다
- 컬럼이 마음에 안 들면 타입이 아니라 **마이그레이션을 고친다**
- 스키마를 바꿨으면 **같은 작업 안에서** 타입을 다시 생성하고 함께 커밋한다
- 생성물이지만 **커밋한다.** 없으면 새로 합류한 사람이 Supabase 로그인 없이는 typecheck조차 못 돌리고, CI도 같은 이유로 막힌다
- 기존 마이그레이션 파일을 수정하지 않는다. 이미 적용된 것은 되돌릴 수 없으므로 새 파일을 만든다

## 9. 검증은 신뢰 경계에서, 권한은 RLS에서

**검증** — 브라우저 쪽 검증은 친절함이지 방어가 아니다. 요청을 직접 보내면 그냥 뚫린다.
서버 진입점(Server Action)에서 **같은 검증을 한 번 더** 한다. 클라이언트 검증만 있는 입력은 검증이 없는 것으로 친다.

**권한** — "이 글이 내 글인가" 같은 소유권 체크를 화면마다, 액션마다 흩뿌리지 않는다.
기능이 늘어나면 반드시 빠뜨리는 경로가 생긴다. **RLS 정책 한 곳**에 쓰고, DB가 모든 경로를 막게 한다.
앱 코드에 같은 체크를 중복으로 두지 않는다(규칙 2). RLS를 못 믿겠으면 코드를 더 쓰지 말고 **정책을 고친다.**

## 10. 화면은 상태 3종이 없으면 미완성이다

성공 경로만 만든 화면을 "됐다"고 하지 않는다.

- **로딩** — 스켈레톤 또는 비활성 상태
- **빈 상태** — 문구 + 다음 행동 유도
- **에러** — 무엇이 잘못됐고 무엇을 하면 되는지

접근성(시맨틱 태그, label, 포커스)은 Biome의 a11y 룰이 CI에서 본다. 별도로 적지 않는다.

## 11. 한 번에 한 기능

한 기능은 **쿼리 → UI → 배포 확인**까지 세로로 관통해서 끝내고 다음으로 넘어간다.
화면을 전부 만들고 나중에 데이터를 붙이는 순서는 금지한다. 마지막에 통합 지옥이 온다.

**1 PR = 1 기능.** 여러 기능이 섞인 PR은 테스트도 리뷰도 어렵다.

## 12. `.env`, `.env.local`을 절대 열지 않는다

**이 규칙에는 예외가 없다.** 사용자가 "봐도 된다"고 하기 전까지, 그리고 말해도 다시 확인하기 전까지 열지 않는다.

- **읽지 않는다** — `cat`, `head`, `sed`, `grep`, 파일 읽기 도구, 그 무엇으로도 내용을 보지 않는다
- **출력하지 않는다** — `echo`, `console.log`, 디버그 출력, 에러 메시지, 커밋, PR 본문, 대화 어디에도 값을 옮겨 적지 않는다
- **필요한 건 값이 아니라 키 이름이다.** 키 목록은 `.env.example`에 있다. 그쪽을 본다
- **코드에서는 `process.env.KEY`로 참조한다.** 값을 상수로 하드코딩하지 않는다
- **존재 여부만 확인한다** — `test -f .env.local`까지다. 안을 들여다보지 않는다
- 값이 필요하면 **사용자에게 요청한다.** 사용자가 붙여넣더라도 파일이나 문서에 저장하지 않는다
- 실수로 봤다면 **봤다고 보고한다.** 그 값을 이후 어디에도 쓰지 않는다

키가 잘못됐는지 의심되면 값을 보지 말고 **증상**으로 좁힌다 — 어떤 요청이 몇 번 에러로 실패하는지.

**복사하지 않는다. 심링크한다.**

작업 디렉터리가 여러 개일 때(별도 체크아웃, 하위 프로젝트 등) `.env.local`을 그쪽으로 복사하지 않는다.
루트의 파일을 심링크로 가리킨다.

```bash
ln -s ../../.env.local <대상>/.env.local
```

복사하면 사본이 늘어나 유출 지점이 늘고, 루트에서 키를 바꿨을 때 사본만 옛 값으로 남는다.
심링크는 원본 하나만 유지한다 — 규칙 3(SOT)이 환경변수에도 그대로 적용된다.
심링크를 만들 때도 파일 **내용은 보지 않는다.**

`.gitignore:34`의 `.env*`가 커밋을 막지만, 그건 실수를 막는 장치지 이 규칙의 근거가 아니다.
Vercel 같은 배포 환경의 환경변수도 같다. 대시보드에 넣는 것은 사용자 몫이고, 에이전트는 키 이름만 안내한다.

## 13. 규칙이나 구조가 바뀌면 문서를 업데이트한다

단, **임의로 고치지 않는다.** 무엇이 어긋났는지 먼저 보고하고, 승인받은 뒤 같은 PR에서 고친다.
낡은 문서는 규칙 3(SOT) 위반이다.

## 14. 갈림길은 기록한다

선택지가 둘 이상이었고 어느 쪽을 골라도 코드가 돌아갔던 결정은 `docs/decisions/`에 남긴다.
**버린 선택지와 그 이유**를 적는다 — 코드는 고른 것만 보여주므로 그건 여기에만 남는다.

- 되돌리는 데 비용이 드는 것(스키마, 인증 방식, 보안 판단)과 **일부러 어긴 규칙**은 반드시 적는다
- 관례적 기본값이 명확했던 선택은 적지 않는다. 코드 주석 한 줄로 끝나는 것도 적지 않는다
- 기존 파일을 고쳐 쓰지 않는다. 마음이 바뀌면 새 번호로 쓰고 옛 파일을 `대체됨`으로 바꾼다
- 형식과 목록은 `docs/decisions/README.md`

---

# 커밋 / 브랜치

## 브랜치

`main`은 항상 배포 가능한 상태를 유지한다. `main`에 직접 커밋하지 않는다.

```
feat/post-create        새 기능
fix/comment-tree-order  버그 수정
chore/ci-cache          설정·빌드·의존성
docs/agents-rules       문서
```

- 형식: `타입/영문-케밥-케이스`
- **브랜치 하나 = 기능 하나** (규칙 11)
- 머지된 브랜치는 지운다

## 히스토리를 다시 쓰지 않는다

**이미 푸시한 것은 고치지 않는다.** 예외 없다.

금지:

- `git push --force`, `-f`, `--force-with-lease`
- 이미 푸시한 커밋에 `git commit --amend`
- 이미 푸시한 브랜치를 `git rebase`
- 푸시된 커밋을 지우는 `git reset --hard`

대신 이렇게 한다:

| 하고 싶은 것 | 대신 |
|---|---|
| 브랜치를 최신 main에 맞추기 | `git merge origin/main` — 머지 커밋이 남지만 남의 참조가 깨지지 않는다 |
| 잘못 올린 커밋 되돌리기 | `git revert <커밋>` — 지우지 말고 되돌리는 커밋을 새로 쌓는다 |
| 커밋 메시지 오타 | 그냥 둔다. 히스토리를 다시 쓸 만큼 중요하지 않다 |

히스토리를 다시 쓰면 남이 받아간 커밋과 어긋나고, PR에 달린 리뷰 코멘트가 떨어져 나간다.
정리된 히스토리보다 **잃어버리지 않는 히스토리**가 낫다.

되돌릴 방법이 없어 보이면 **멈추고 사용자에게 묻는다.**

## 커밋 메시지

```
타입: 한 줄 요약

왜 이렇게 했는지. 무엇을 했는지가 아니라.
(한 줄로 충분하면 본문은 생략)
```

| 타입 | 용도 |
|------|------|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변화 없는 구조 변경 |
| `style` | 화면·토큰·스타일 |
| `chore` | 설정, 빌드, 의존성 |
| `docs` | 문서 |
| `test` | 테스트 |

- 요약은 한국어, 마침표 없이, 50자 이내
- 커밋 하나는 **되돌릴 수 있는 단위**여야 한다. "작업중" 커밋을 남기지 않는다
- 커밋 전에 `npm run verify`를 돌린다

## PR

본문은 `.github/PULL_REQUEST_TEMPLATE.md`를 그대로 채운다 — 무엇을 / 어떻게 / 왜 / 트레이드오프 / 참고사항 / 체크리스트.
**항목을 지우지 않는다.** 해당 없으면 "없음"이라고 쓴다. 빈칸으로 두면 안 쓴 것인지 없는 것인지 리뷰어가 모른다.

- 제목은 커밋 메시지와 같은 형식
- CI가 초록이고, Vercel 프리뷰 URL에서 직접 눌러본 뒤에 머지한다
- 머지는 **머지 커밋**으로 한다. 레포 설정에서 `Allow merge commits`만 켜고 squash·rebase는 끈다 ([결정 0005](docs/decisions/0005-merge-commits.md))

PR 만드는 절차는 `/pr` 스킬(`.claude/skills/pr/SKILL.md`)에 있다. 사람이 손으로 해도 같은 순서다.
리뷰에 **대응하는** 절차는 `/pr-review`(`.claude/skills/pr-review/SKILL.md`)로 따로 나뉘어 있다 —
PR을 만드는 시점과 리뷰를 보는 시점이 다르기 때문이다.

**자동 리뷰.** PR을 열면 Claude가 `AGENTS.md` 기준으로 한 번 리뷰한다
(`.github/workflows/claude-review.yml`). 다시 보고 싶으면 PR에 `@claude` 를 멘션한다.
푸시마다 돌지 않으므로, 지적을 고친 뒤 다시 받고 싶으면 직접 불러야 한다.
리뷰는 사람 리뷰를 대신하지 않는다. **리뷰어도 틀린다** — 지적을 먼저 레포에서 검증하고,
근거가 없으면 무시하되 왜 무시했는지 답글을 남긴다. 반박한 스레드는 스스로 닫지 않는다.

---

# 환경변수

`.env.local` (커밋 금지, `.env.example` 참고)

| 키 | 용도 |
|----|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 익명 키 |

---

# 아직 정하지 않은 것

없음. 새로 생기면 여기에 적고, 정해지기 전까지 **임의로 정해서 진행하지 않는다**(규칙 6).
