<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# 8around SNS

생각을 짧게 나누는 SNS. 3일 안에 배포까지 간다.

**이 문서가 개발 규칙의 유일한 출처다.** 사람이든 AI 에이전트든 이 문서를 읽고 작업한다.
`CLAUDE.md`는 이 파일을 가리키기만 한다. 같은 규칙을 두 곳에 적지 않는다.

- 일정: `docs/PLAN.md`
- 디자인 토큰: `docs/DESIGN.md`
- 결정 기록: `docs/decisions/`. 갈림길에서 무엇을 버렸는지

## 범위

요구 기능은 **6개**다. 이것이 전부다.

회원가입, 로그인, 팔로우와 팔로잉, 게시글 작성, 댓글, 대댓글

좋아요는 요구 기능이 아니지만 사용자가 요청해서 범위에 들어왔다. **게시글에만** 단다,
댓글에는 달지 않는다 ([결정 0020](docs/decisions/0020-post-likes.md)).
다크 모드도 사용자가 요청해서 들어왔다. 더 보기의 디자인 메뉴에서 고른다 ([결정 0040](docs/decisions/0040-theme-switch.md)).

범위 밖(알림, DM, 이미지 업로드, 검색, 해시태그 등)은 **사용자 요청이 없으면 구현하지 않는다.**

## 명령어

| 명령 | 용도 |
|------|------|
| `npm run verify` | lint, typecheck, test, harness. 커밋 전에 이것만 돌리면 된다 |
| `npm run format` | Biome 포맷 + 자동 수정 |
| `npm run dev` | 개발 서버. **에이전트가 직접 실행하지 않는다 (규칙 7)** |
| `npm run db:push` | 마이그레이션을 원격 DB에 적용. 규칙 7에 따라 먼저 묻는다 |
| `npm run types:gen` | 스키마에서 `types/database.ts` 재생성 |
| `npm run harness` | 규칙 중 기계가 볼 수 있는 것만 검사한다. `verify`가 부른다 |
| `npm run test:e2e` | 떠 있는 개발 서버에 대고 심사 시나리오 E2E를 돌린다. 실제 DB에 계정이 남으므로 CI 실패를 재현할 때만 쓴다 (규칙 17) |

CI는 `verify` 3단계에 `npm run build`를 더해 그대로 돌린다. 로컬에서 `verify`가 통과하면 CI도 통과한다.
E2E는 CI의 다른 job이 러너 안에 띄운 로컬 Supabase에 대고 돌린다 ([결정 0048](docs/decisions/0048-e2e-against-remote-db.md)).

## 구조

**역할별로 나눈다.** 파일을 어디에 만들지는 "이게 무슨 역할인가"로 결정한다.

```
app/                    라우트. page.tsx / layout.tsx 만. 로직을 두지 않는다
components/
  ui/                   순수 UI. props만 받는다. 데이터 접근도 상태도 없다
  layout/               화면 뼈대. 여러 화면이 공유하는 네비게이션과 컨테이너
  <기능>/               그 기능에만 쓰이는 UI. post/, comment/, follow/
hooks/                  클라이언트 훅 (use~)
lib/
  supabase/             Supabase 클라이언트 생성
  queries/              읽기. 서버에서만 호출한다
  actions/              쓰기 진입점. "use server" 얇은 래퍼. 인증 확인 후 services 호출
  services/             쓰기 로직. 검증 + DB. 프레임워크를 모른다
  utils/                순수 함수. 옆에 *.test.ts 를 둔다
types/                  스키마에서 생성한 DB 타입 (규칙 8)
supabase/migrations/    스키마. SQL 파일만이 출처 (규칙 3)
scripts/                verify가 부르는 검사. 앱 코드가 아니다
e2e/                    심사 시나리오 E2E. *.e2e.ts (규칙 17)
docs/                   PLAN.md, DESIGN.md
  decisions/            결정 기록. 선택지와 버린 이유 (규칙 14)
```

**어디에 둘지 헷갈릴 때**

| 이건 | 여기 |
|------|------|
| 여러 기능에서 쓸 버튼, 아바타, 태그 | `components/ui/` |
| 게시글에서만 쓰는 카드 | `components/post/` |
| `useState`가 두 군데 이상에서 같은 모양으로 반복될 때 | `hooks/` |
| 입력만으로 결과가 정해지는 함수 | `lib/utils/` + 테스트 |
| DB를 읽는다 | `lib/queries/` |
| DB를 쓴다 | `lib/services/` (로직) + `lib/actions/` (진입점) |

디렉터리는 **첫 파일이 생길 때 만든다.** 빈 폴더를 미리 파두지 않는다.

## 쓰기는 Server Action으로만

API 라우트를 만들지 않는다. 생성, 수정, 삭제는 전부 Server Action이다.

**로직은 진입점에 눌러붙이지 않는다.** 두 층으로 나눈다.

```
lib/actions/post.ts     "use server". 로그인 확인, services 호출, revalidate 순서
lib/services/post.ts    검증(zod) + DB 쓰기. Next를 모른다. 테스트하기 쉽다
```

이유: 유지보수 비용은 "Next를 버릴 때"가 아니라 **로직이 진입점에 붙어서 재사용도 테스트도 안 될 때** 생긴다.
나중에 외부에서 호출할 API가 필요해지면 Route Handler가 같은 `services/` 함수를 부르면 된다. 재작성이 아니라 진입점 추가다.

## 스타일

Tailwind v4를 쓴다. 유틸리티를 화면에 직접 흩뿌리지 않고 공통 컴포넌트에 가둔다.

- 토큰 값의 출처는 `docs/DESIGN.md`이고, `app/globals.css`가 그것을 그대로 옮겨 적는다
- `@theme`로 Tailwind 유틸리티에 노출한다. `bg-canvas`, `text-fg`, `text-body`, `rounded-md`, `border-hairline`
- 간격은 Tailwind 숫자 스케일을 쓴다. 그것이 곧 4px 그리드다. `p-1`은 4px, `p-4`는 16px, `p-16`은 64px
- 임의의 값을 쓰지 않는다. `p-[13px]`, `text-[#333]` 같은 것이다. 6px이 필요하면 `p-1.5`를 쓴다.
  그리드 밖의 값이 꼭 필요하면 이유를 주석으로 남긴다
- 컴포넌트를 어디에 둘지는 규칙 2를 따른다. 화면 전용이면 기능 폴더, 어디서나 쓰일 성격이면 `components/ui/`
- 컴포넌트를 만들면 `docs/DESIGN.md`의 Components & States가 요구하는 상태를 다 채운다.
  loading, disabled, pressed, keyboard focus 넷이다. 하나라도 비면 미완성이다
- 화면 문구는 `docs/DESIGN.md`에 없다. 새로 쓸 때는 Voice & Tone 절에 맞추고, 이미 있는 화면의 말투를 따른다
- 문서에 없는 값을 지어내지 않는다. 필요한데 없으면 멈추고 묻는다
- 라이트와 다크를 둘 다 지원한다. 색 토큰은 `app/globals.css`에 `light-dark(라이트, 다크)` 한 줄로 적고,
  화면 코드에서는 `dark:` 변형이나 `prefers-color-scheme` 분기를 쓰지 않는다 ([결정 0040](docs/decisions/0040-theme-switch.md))
- 포커스와 키보드를 다뤄야 하는 것은 직접 만들지 않는다. 모달, 시트, 드롭다운, 탭이 그렇다.
  shadcn 소스를 가져와 우리 토큰에 맞춘다. 클래스를 합칠 때는 `lib/utils/cn.ts`의 `cn()`을 쓴다
- 아이콘은 `lucide-react`에서 가져온다. SVG를 손으로 그리지 않는다.
  `aria-hidden`은 직접 붙인다. lucide가 안 붙인다

소프트 서피스 디자인에서 오는 제약이다. 근거는 `docs/DESIGN.md`와 [결정 0043](docs/decisions/0043-soft-surface.md)에 있다.

- 보라(`primary`)는 동작과 현재 위치에만 칠한다. 장식으로 쓰지 않는다. 관심사 톤은 칩과 아바타에만 쓴다
- 글꼴은 `app/fonts/`의 Pretendard 하나다([결정 0044](docs/decisions/0044-renewal-open-decisions.md)). 다른 웹폰트는 결정을 거치지 않고 넣지 않는다
- 새 이징이나 duration을 만들지 않는다. `--motion-*`과 `--ease-*` 셋만 쓴다. 스프링과 오버슈트는 금지다
- 그림자는 라이트에서 `shadow-card`, `shadow-raised` 둘만 쓴다. 다크는 그림자 없이 면 색으로 나눈다
- 한글 본문은 단어 단위로 줄바꿈한다. `break-keep`과 `wrap-anywhere`를 같이 둔다

---

# 규칙

제목에 `(에이전트)`가 붙은 5번, 6번, 7번, 12번, 16번은 에이전트가 사용자와 일할 때의 규칙이다.
나머지는 사람과 에이전트가 같이 지킨다.

기계가 볼 수 있는 것은 `npm run verify`가 본다. 나머지는 PR 리뷰가 본다.

## 1. 함수는 찾고, 따지고, 분리한다

새 함수를 파일 안에 바로 쓰지 않는다. 순서는 이렇다.

1. 찾는다. 같은 일을 하는 함수가 이미 있는지 확인한다. 있으면 그것을 쓴다.
2. 따진다. 입력만으로 결과가 정해지는가, 이 화면 밖에서도 쓰이는가.
3. 분리한다. 둘 다 맞으면 `lib/utils/`에 파일로 빼고 옆에 `*.test.ts`를 둔다.

한 화면의 표현에만 묶여 있으면 그 파일에 남긴다. 남긴 이유를 한 줄로 말한다 (규칙 4).

## 2. 같은 것을 두 번 만들지 않는다

새로 만들기 전에 `components/`와 `lib/`에서 같은 일을 하는 것을 먼저 찾는다.

컴포넌트를 어디에 둘지는 횟수가 아니라 성격으로 정한다.

- 한 화면에서만 쓰는 것은 그 기능 폴더에 둔다. `components/post/`, `components/auth/`
- 버튼, 아바타, 입력칸처럼 어느 화면에서나 쓰일 성격은 `components/ui/`에 둔다
- 애매하면 기능 폴더에 두고, 두 번째 화면이 쓰기 시작할 때 `components/ui/`로 올린다

같은 모양을 복사해서 두 벌로 두지 않는다. 올릴 때는 옮기고, 옛 자리를 지운다.

## 3. 사실은 한 곳에만 적는다

같은 사실이 두 곳에 적혀 있으면 둘 다 틀린 것으로 친다.

| 사실 | 유일한 출처 |
|------|-------------|
| DB 스키마 | `supabase/migrations/*.sql`. 대시보드에서 손으로 고치지 않는다 |
| DB 타입 | `types/database.ts`. `npm run types:gen`으로 생성한다 (규칙 8) |
| 색, 간격, 폰트 | `docs/DESIGN.md` 토큰 |
| 개발 규칙 | 이 문서 |
| 일정, 범위 | `docs/PLAN.md` |
| 결정과 그 이유 | `docs/decisions/NNNN-*.md` (규칙 14) |

문서에 없는데 코드에 남긴 값은 `app/globals.css`에 "로컬 확장"이라고 적고 이유를 붙인다.
그 주석이 그 값의 출처가 된다.

## 4. 근거 없이 코드를 쓰지 않는다

추측으로 쓰지 않는다. 근거는 이 레포 안에서 찾는다.

- 근거의 형태는 `파일:줄`, 또는 실행한 명령과 그 출력이다
- "보통 이렇게 합니다", "일반적으로", "아마"는 근거가 아니다
- 외부 블로그, 기억, 관례를 근거로 삼지 않는다
- 레포에서 확인하지 못했으면 확인하지 못했다고 그대로 보고한다. 모른다고 말하는 것이 지어내는 것보다 싸다

## 5. 사실만 보고한다 (에이전트)

했으면 했다고, 안 했으면 안 했다고 쓴다. 테스트가 실패하면 실패했다고 출력과 함께 쓴다.
"될 겁니다", "아마 동작합니다"는 보고가 아니다. 실행해서 확인한 것만 동작한다고 말한다.

## 6. 지시받기 전까지 임의로 진행하지 않는다 (에이전트)

방향이 갈리는 지점에서는 멈춘다. 멈출 때는 이 형태로 말한다.

```
근거: (파일:줄 또는 명령 출력)
선택지 A. 장점과 비용
선택지 B. 장점과 비용
추천: A. 이유 한 줄.
```

혼자 정하고 진행한 뒤 통보하지 않는다. 관례적인 기본값이 명확한 사소한 선택은 진행하되,
무엇을 가정했는지 한 줄로 밝힌다.

## 7. 사용자 환경을 건드리는 명령은 먼저 묻는다 (에이전트)

에이전트는 개발 서버를 임의로 실행하거나 끄지 않는다. 화면 확인이 필요하면 사용자에게 요청한다.

물어보고 실행하는 것들.

- 개발 서버 실행과 종료, 포트 점유
- DB 마이그레이션 적용, 리셋, 시드
- `git push`, 브랜치 전환. 히스토리를 다시 쓰는 명령은 묻지 말고 하지 않는다
- 패키지 설치와 제거

환경변수 파일은 실행이 아니라 열람 자체가 금지다 (규칙 12).

## 8. 스키마가 타입의 출처다

DB 타입은 스키마에서 생성한다. 손으로 쓴 타입과 실제 컬럼이 어긋나면 런타임에서만 터진다.

```
supabase/migrations/*.sql   스키마를 바꾸는 유일한 방법. 새 파일을 추가한다
npm run db:link             원격 프로젝트에 연결한다. 아래 두 명령의 전제다
npm run db:push             원격 DB에 적용
npm run types:gen           types/database.ts 재생성
```

- `types/database.ts`를 손으로 고치지 않는다. 생성물이라 다음 `types:gen` 때 날아간다
- 컬럼이 마음에 안 들면 타입이 아니라 마이그레이션을 고친다
- 스키마를 바꿨으면 같은 작업 안에서 타입을 다시 생성하고 함께 커밋한다
- 생성물이지만 커밋한다. 없으면 새로 합류한 사람이 Supabase 로그인 없이 typecheck조차 못 돌리고, CI도 같은 이유로 막힌다
- 기존 마이그레이션 파일을 수정하지 않는다. 이미 적용된 것은 되돌릴 수 없으므로 새 파일을 만든다

## 9. 검증은 신뢰 경계에서, 권한은 RLS에서

브라우저 쪽 검증은 친절함이지 방어가 아니다. 요청을 직접 보내면 그냥 뚫린다.
서버 진입점에서 같은 검증을 한 번 더 한다. 클라이언트 검증만 있는 입력은 검증이 없는 것으로 친다.

권한은 다르다. "이 글이 내 글인가" 같은 소유권 체크를 화면마다, 액션마다 흩뿌리지 않는다.
기능이 늘어나면 반드시 빠뜨리는 경로가 생긴다. RLS 정책 한 곳에 쓰고 DB가 모든 경로를 막게 한다.
앱 코드에 같은 체크를 중복으로 두지 않는다 (규칙 2). RLS를 못 믿겠으면 코드를 더 쓰지 말고 정책을 고친다.

## 10. 화면은 상태 네 가지가 없으면 미완성이다

성공 경로만 만든 화면을 됐다고 하지 않는다. 상태마다 자리가 정해져 있다.

| 상태 | 어디에 |
|---|---|
| 로딩 | 그 라우트에 `loading.tsx`. 화면 전환이 없는 폼은 버튼 비활성으로 대신한다 |
| 빈 상태 | 목록이 비면 `components/ui/EmptyState.tsx`로 문구와 다음 행동을 보여준다 |
| 없음 | 주소가 가리키는 것이 없으면 그 라우트에 `not-found.tsx` |
| 에러 | `app/error.tsx`가 받는다. 화면마다 따로 만들지 않는다 |

접근성은 Biome의 a11y 룰이 CI에서 본다. 여기에 따로 적지 않는다.

## 11. 한 번에 한 기능

한 기능은 쿼리에서 UI, 배포 확인까지 세로로 관통해서 끝내고 다음으로 넘어간다.
화면을 전부 만들고 나중에 데이터를 붙이는 순서는 금지한다. 마지막에 통합 지옥이 온다.

1 PR은 1 기능이다. 여러 기능이 섞인 PR은 테스트도 리뷰도 어렵다.

기능이 아닌 변경은 예외다. 토큰 교체, 의존성 추가, 규칙 문서 수정처럼 여러 기능에 걸치는 것은
쪼개면 중간 상태가 더 이상해진다. 묶었으면 PR 본문에 왜 묶었는지 적는다.

## 12. `.env`, `.env.local`을 열지 않는다 (에이전트)

이 규칙에는 예외가 없다.

- 읽지 않는다. `cat`, `grep`, 파일 읽기 도구 그 무엇으로도 내용을 보지 않는다
- 옮겨 적지 않는다. 로그, 에러 메시지, 커밋, PR 본문, 대화 어디에도 값을 쓰지 않는다
- 필요한 것은 값이 아니라 키 이름이다. 키 목록은 `.env.example`에 있다
- 코드에서는 `process.env.KEY`로 참조한다. 값을 상수로 하드코딩하지 않는다
- 존재 확인은 `test -f .env.local`까지다
- 값이 필요하면 사용자에게 요청한다. 받은 값을 파일이나 문서에 저장하지 않는다
- 실수로 봤으면 봤다고 보고하고, 그 값을 이후 어디에도 쓰지 않는다

키가 잘못됐는지 의심되면 값을 보지 말고 증상으로 좁힌다. 어떤 요청이 몇 번 에러로 실패하는지 본다.

배포 환경의 환경변수도 같다. 대시보드에 넣는 것은 사용자 몫이고, 에이전트는 키 이름만 안내한다.

## 13. 규칙이나 구조가 바뀌면 문서를 고친다

임의로 고치지 않는다. 무엇이 어긋났는지 먼저 보고하고, 승인받은 뒤 같은 PR에서 고친다.
낡은 문서는 규칙 3 위반이다.

## 14. 갈림길은 기록한다

선택지가 둘 이상이었고 어느 쪽을 골라도 코드가 돌아갔던 결정은 `docs/decisions/`에 남긴다.
버린 선택지와 그 이유를 적는다. 코드는 고른 것만 보여주므로 그것은 여기에만 남는다.

- 되돌리는 데 비용이 드는 것과 일부러 어긴 규칙은 반드시 적는다. 스키마, 인증 방식, 보안 판단이 그렇다
- 관례적 기본값이 명확했던 선택은 적지 않는다. 코드 주석 한 줄로 끝나는 것도 적지 않는다
- 기존 파일을 고쳐 쓰지 않는다. 마음이 바뀌면 새 번호로 쓰고 옛 파일의 상태를 `대체됨`으로 바꾼다
- 형식과 목록은 `docs/decisions/README.md`

## 15. 주석은 TSDoc으로 쓰고 사실만 적는다

주석이 코드보다 길면 다음 사람은 무엇을 믿어야 할지 모른다.

- 내보내는 함수, 컴포넌트, 타입에만 `/** */`을 단다. 파일 안에서만 쓰는 것에는 달지 않는다
- 무엇을 하는지와 제약만 적는다. 어떻게 판단했는지는 적지 않는다. 그것은 결정 기록이 받는다
- 근거는 레포 안의 파일 경로나 결정 번호로 가리킨다. `@see lib/queries/comment.ts` 또는 `결정 0008`
- 문서의 절 번호를 인용하지 않는다. 문서가 바뀌어도 코드는 통과하므로 주석만 조용히 거짓이 된다
- 코드를 읽으면 아는 것은 적지 않는다
- 줄 주석은 그 줄이 이상해 보이는 이유가 있을 때만, 한 줄로 단다
- 이모지와 장식 기호를 쓰지 않는다. 나열은 쉼표로 한다. 이 규칙은 문서와 커밋 메시지에도 같다

## 16. 화면은 열어서 확인한다 (에이전트)

`.mcp.json`에 등록된 Playwright MCP로 화면을 직접 열어 동작을 점검한다.
코드만 읽고 "될 것"이라고 보고하지 않는다 (규칙 5).

- 개발 서버와 브라우저 설치(`npx playwright install chromium`)는 규칙 7을 따른다
- 보고는 무엇을 눌렀고 무엇이 보였는지로 한다. 근거는 스크린샷과 콘솔 출력이다 (규칙 4)

## 17. 사용자 흐름을 바꾸면 E2E를 같이 고친다

단위 테스트는 가짜 DB로 돌아 화면 흐름이 끊긴 것을 잡지 못한다.
답글이 없는 댓글에서 답글 화면으로 가는 길이 사라진 것을 E2E를 짜면서야 알았다 ([결정 0047](docs/decisions/0047-reply-entry-without-replies.md)).

- 대상은 `e2e/signup-flow.e2e.ts`가 지나는 흐름이다. 가입, 로그인, 로그아웃, 글 작성, 댓글, 답글, 팔로우
- 그 흐름의 화면 구성, 버튼과 입력칸의 이름, 주소를 바꿨으면 같은 PR에서 E2E를 고친다
- 모든 화면을 E2E로 덮지 않는다. 입력 오류와 실패 처리는 `lib/services/*.test.ts`가 본다
- 확인은 PR의 CI `e2e` job 결과로 한다. CI는 러너 안의 테스트 DB에 대고 돈다
- 로컬 `npm run test:e2e`는 CI 실패를 재현할 때만 쓴다. 실제 DB에 빈 계정 둘이 남는다 ([결정 0049](docs/decisions/0049-e2e-ci-first.md)). 개발 서버는 규칙 7을 따른다

---

# 커밋 / 브랜치

## 브랜치

`main`은 항상 배포 가능한 상태를 유지한다. `main`에 직접 커밋하지 않는다.

```
feat/post-create        새 기능
fix/comment-tree-order  버그 수정
chore/ci-cache          설정, 빌드, 의존성
docs/agents-rules       문서
```

- 형식: `타입/영문-케밥-케이스`
- **브랜치 하나 = 기능 하나** (규칙 11)
- 머지된 브랜치는 지운다

## 히스토리를 다시 쓰지 않는다

**이미 푸시한 것은 고치지 않는다.** 예외 없다.

금지:

- `git push --force`, `-f`, `--force-with-lease`
- 이미 푸시한 커밋에 `git commit --amend`
- 이미 푸시한 브랜치를 `git rebase`
- 푸시된 커밋을 지우는 `git reset --hard`

대신 이렇게 한다:

| 하고 싶은 것 | 대신 |
|---|---|
| 브랜치를 최신 main에 맞추기 | `git merge origin/main`. 머지 커밋이 남지만 남의 참조가 깨지지 않는다 |
| 잘못 올린 커밋 되돌리기 | `git revert <커밋>`. 지우지 말고 되돌리는 커밋을 새로 쌓는다 |
| 커밋 메시지 오타 | 그냥 둔다. 히스토리를 다시 쓸 만큼 중요하지 않다 |

히스토리를 다시 쓰면 남이 받아간 커밋과 어긋나고, PR에 달린 리뷰 코멘트가 떨어져 나간다.
정리된 히스토리보다 **잃어버리지 않는 히스토리**가 낫다.

되돌릴 방법이 없어 보이면 **멈추고 사용자에게 묻는다.**

## 커밋 메시지

```
타입: 한 줄 요약

왜 이렇게 했는지. 무엇을 했는지가 아니라.
(한 줄로 충분하면 본문은 생략)
```

| 타입 | 용도 |
|------|------|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변화 없는 구조 변경 |
| `style` | 화면, 토큰, 스타일 |
| `chore` | 설정, 빌드, 의존성 |
| `docs` | 문서 |
| `test` | 테스트 |

- 요약은 한국어, 마침표 없이, 50자 이내
- 커밋 하나는 **되돌릴 수 있는 단위**여야 한다. "작업중" 커밋을 남기지 않는다
- 커밋 전에 `npm run verify`를 돌린다

## PR

본문은 `.github/PULL_REQUEST_TEMPLATE.md`를 그대로 채운다. 무엇을, 어떻게, 왜, 트레이드오프, 참고사항, 체크리스트.
**항목을 지우지 않는다.** 해당 없으면 "없음"이라고 쓴다. 빈칸으로 두면 안 쓴 것인지 없는 것인지 리뷰어가 모른다.

- 제목은 커밋 메시지와 같은 형식
- CI가 초록이고, Vercel 프리뷰 URL에서 직접 눌러본 뒤에 머지한다
- 머지는 머지 커밋으로 한다. 레포 설정에서 `Allow merge commits`만 켜고 squash와 rebase는 끈다 ([결정 0005](docs/decisions/0005-merge-commits.md))

PR 만드는 절차는 `/pr` 스킬(`.claude/skills/pr/SKILL.md`)에 있다. 사람이 손으로 해도 같은 순서다.
리뷰에 대응하는 절차는 `/pr-review`(`.claude/skills/pr-review/SKILL.md`)로 따로 나뉘어 있다.
PR을 만드는 시점과 리뷰를 보는 시점이 다르기 때문이다.

**자동 리뷰.** PR을 열면 Claude가 `AGENTS.md` 기준으로 한 번 리뷰한다
(`.github/workflows/claude-review.yml`). 다시 보고 싶으면 PR에 `@claude` 를 멘션한다.
푸시마다 돌지 않으므로, 지적을 고친 뒤 다시 받고 싶으면 직접 불러야 한다.
리뷰는 사람 리뷰를 대신하지 않는다. 리뷰어도 틀린다. 지적을 먼저 레포에서 검증하고,
근거가 없으면 무시하되 왜 무시했는지 답글을 남긴다. 반박한 스레드는 스스로 닫지 않는다.

---

# 환경변수

`.env.local` (커밋 금지, `.env.example` 참고)

| 키 | 용도 |
|----|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 익명 키 |
