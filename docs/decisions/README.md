# 결정 기록

갈림길에서 무엇을 고르고 **무엇을 버렸는지** 적는다. 코드는 고른 것만 보여주므로,
버린 선택지와 그 이유는 여기에만 남는다. 없으면 같은 논의를 몇 달 뒤에 다시 한다.

## 무엇을 적나

- **선택지가 둘 이상이었고**, 어느 쪽을 골라도 코드가 돌아갔던 것
- 되돌리려면 비용이 드는 것 — 스키마, 인증 방식, 보안 판단
- 규칙을 일부러 어긴 것과 그 값

관례적인 기본값이 명확했던 선택은 적지 않는다. 코드 주석 한 줄로 끝나는 것도 적지 않는다.
(예: "에러 문자열을 파싱하지 않고 DB 상태로 판정한다" → `lib/services/auth.ts` 주석)

## 어떻게 적나

`NNNN-영문-케밥-제목.md`. 번호는 이어 붙이고 **기존 파일을 고쳐 쓰지 않는다.**
마음이 바뀌면 새 파일을 쓰고 옛 파일의 상태를 `대체됨 → NNNN`으로 바꾼다.
결정을 지우면 "왜 바꿨는지"가 같이 사라진다.

| 번호 | 제목 | 상태 |
|---|---|---|
| [0001](0001-login-identifier.md) | 로그인 식별자는 이메일이다 | 확정 |
| [0002](0002-email-confirmation-off.md) | 이메일 확인을 끈다 | 확정 |
| [0003](0003-signup-email-enumeration.md) | 가입 시 이메일 중복을 그대로 알린다 | 확정 |
| [0004](0004-auth-single-screen-slide.md) | 가입·로그인을 한 화면에 놓고 미끄러뜨린다 | 확정 |
